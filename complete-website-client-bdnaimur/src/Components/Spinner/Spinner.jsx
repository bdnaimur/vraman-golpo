import React from 'react';
import spinner from '../../assets/spinner/spinner.gif';

const Spinner = () => {
    return (
        <div className="text-center"><img src={spinner} alt="Spinner" srcset="" /></div>
    );
};

export default Spinner;